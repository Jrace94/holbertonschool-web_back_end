#!/usr/bin/env python3
"""Module that presents the concat function with annotations"""


def concat(str1: str, str2: str) -> str:
    """Function that returns the concatenation of two text strings"""
    return str1 + str2
