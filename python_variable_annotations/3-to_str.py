#!/usr/bin/env python3
"""Module that presents the to_str function with annotations"""


def to_str(n: float) -> str:
    """Function that returns the floating number `n` in a text string"""
    return str(n)
